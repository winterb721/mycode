// Import the necessary React libraries
import React from 'react';
import ReactDOM from 'react-dom';

// Define a simple React component that displays a message
const App = () => <h1>Hello from custom Webpack config!</h1>;

// Render the App component inside the HTML element with the id 'root'
ReactDOM.render(<App />, document.getElementById('root'));
